import 'package:flutter/material.dart';

class AfterCareTips extends StatelessWidget {
  const AfterCareTips({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
